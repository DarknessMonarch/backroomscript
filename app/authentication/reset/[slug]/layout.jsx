const SITE_URL = "http://backroomscript.com";

export const metadata = {
  title: "Set New Password - Reset Your Account Password",
  description: "Set a new password for your BackroomScript account to restore access to your conversation templates.",

  robots: {
    index: false,
    follow: false,
  },
};

export default function ResetSlugLayout({ children }) {
  return <>{children}</>;
}
